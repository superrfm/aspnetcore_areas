@model aspnetcore_areas.Models.LoginViewModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<h4>LoginViewModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="GravarCadastro">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Perfil" class="control-label"></label>
                <select asp-for="Perfil">
                    <option value="Medico" selected="selected">Médico</option>
                    <option value="Paciente">Paciente</option>
                </select>
                <span asp-validation-for="Perfil" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Redirect1" class="btn btn-primary" />
            </div>
        </form>
        <form asp-action="MesmoControllerName">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Perfil" class="control-label"></label>
                <select asp-for="Perfil">
                    <option value="Medico" selected="selected">Médico</option>
                    <option value="Paciente">Paciente</option>
                </select>
                <span asp-validation-for="Perfil" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Redirect2" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

